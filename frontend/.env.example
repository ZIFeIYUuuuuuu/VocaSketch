# VocaSketch frontend runtime config.
# Browser Web Speech remains the local ASR path; command parsing prefers backend with local fallback.

VITE_API_BASE_URL=http://localhost:4000/api/v1
VITE_ENABLE_MOCK_COMMANDS=true
