# VocaSketch frontend runtime config.
# The current PR uses a local mock parser and browser speech APIs.
# Backend integration will be wired in a later PR.

VITE_API_BASE_URL=http://localhost:8787/api/v1
VITE_ENABLE_MOCK_COMMANDS=true
