# 仓库协作规则

## Git 操作权限

- 未经用户明确允许，不得执行 `git commit`、`git push`、创建 PR、关闭 PR、合并 PR 或修改远程分支等会改变 Git/GitHub 状态的操作。
- 可以执行只读检查命令，例如 `git status`、`git log`、`git diff`、`gh pr list`、`gh pr view`。
- 如需提交、推送或操作 PR，必须先向用户说明将执行的具体动作，并等待用户明确授权。
